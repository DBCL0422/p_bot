services:
  - type: web
    name: polymarket-edge-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: BANKROLL
        value: 1000
      - key: MIN_EDGE_PCT
        value: 5
